#!/usr/bin/env python3
"""Build Recipe Hub v119 from the immutable v118 bundle plus the v119 UI layer."""
from pathlib import Path
import re
import sys

FRONTEND = Path(__file__).resolve().parents[1] / "custom_components/cook4me/frontend"
BASE = FRONTEND / "cook4me-panel-v118-bundle.js"
SOURCE = FRONTEND / "cook4me-panel-v119.js"
TARGET = FRONTEND / "cook4me-panel-v119-bundle.js"
IMPORT = re.compile(r'^import ["\']\./cook4me-panel-v118\.js["\'];\s*$', re.M)


def bundle() -> str:
    base = BASE.read_text(encoding="utf-8")
    base = base.replace(
        "// Generated by tools/build_frontend_bundle_v118.py from immutable v117 bundle plus v118 source.",
        "// Generated by tools/build_frontend_bundle_v119.py from immutable v118 bundle plus v119 source.",
        1,
    )
    base = base.replace(
        'const activeElement="cook4me-recipe-hub-panel-v118";',
        'const activeElement="cook4me-recipe-hub-panel-v119";',
        1,
    )
    base = base.replace("-build-202609184", "-build-202609185", 1)
    source = IMPORT.sub("", SOURCE.read_text(encoding="utf-8"), count=1)
    return base.rstrip() + "\n// cook4me-panel-v119.js\n(() => {\n" + source + "\n})();\n"


if __name__ == "__main__":
    value = bundle()
    if "--check" in sys.argv:
        if not TARGET.exists() or TARGET.read_text(encoding="utf-8") != value:
            raise SystemExit(
                "The v119 bundle needs regeneration: python tools/build_frontend_bundle_v119.py"
            )
    else:
        TARGET.write_text(value, encoding="utf-8")
    print("v119 frontend bundle is current")
